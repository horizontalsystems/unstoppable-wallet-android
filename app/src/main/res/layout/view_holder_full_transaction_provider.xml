<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/sectionProviderLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingLeft="16dp"
        android:paddingTop="16dp"
        android:paddingRight="16dp"
        android:paddingBottom="5dp"
        android:text="@string/FullInfo_SubtitleProvider"
        android:textColor="@color/grey"
        android:textSize="14sp" />

    <io.horizontalsystems.bankwallet.modules.transactions.transactionInfo.TransactionInfoItemView
        android:id="@+id/sectionProvider"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/sectionProviderLabel"
        android:paddingBottom="32dp"
        android:textColor="@color/yellow_crypto"
        app:title="Title"
        app:value="Value" />

</RelativeLayout>
